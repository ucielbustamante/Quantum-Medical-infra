global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'Quantum-Medical-backend'
    static_configs:
      - targets: ['backend:5000']
    relabel_configs:
      - source_labels: [__name__, path]
        regex: http_requests_total;/metrics
        action: drop
